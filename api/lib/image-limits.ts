/* ═══════════════════════════════════════════════════════════════════════════
   Сколько знаков помещается в поле картинки.

   Логотип и значок вкладки хранятся строкой data:image/…;base64,… в столбцах
   типа TEXT — это 65 535 БАЙТ, а не «сколько понадобится». Строка длиннее не
   записывается: MySQL отклоняет весь запрос целиком.

   Числа стоят отдельным модулем, потому что поля два, столбца три и окон
   тоже два. Пока предел знал только «Брендинг», вкладка «Компания» жала
   логотип общей меркой — до семисот тысяч знаков — и клала его в тот же по
   устройству столбец. Сохранение падало, и вместе с логотипом не сохранялись
   ни название организации, ни адрес, ни банковские реквизиты; на экране при
   этом стояло безымянное «Внутренняя ошибка сервера».

   На клиенте те же числа лежат в src/lib/compress-image.ts (LOGO_LIMITS и
   FAVICON_LIMITS), и расхождение между ними ловит тест
   api/__tests__/logo-fits-its-column.test.ts.
   ═══════════════════════════════════════════════════════════════════════════ */

/** Ёмкость столбца типа TEXT в MySQL, байт. */
export const TEXT_COLUMN_BYTES = 65_535;

export const LOGO_MAX_CHARS    = 60_000;
export const FAVICON_MAX_CHARS = 30_000;
