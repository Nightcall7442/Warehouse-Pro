UPDATE `payments` SET `received_at` = `created_at` WHERE `payment_method` = 'cash' AND `received_at` IS NULL;
-- История до расчёта по заказу (18.09.2026): наличные, записанные раньше,
-- считаются полученными в момент записи — иначе они легли бы «на руки»
-- полевым. Идемпотентно: повтор после обрыва ничего не меняет.