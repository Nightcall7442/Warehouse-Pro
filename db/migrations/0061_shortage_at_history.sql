UPDATE `orders` SET `shortage_at` = `closed_at` WHERE `courier_shortage` > 0 AND `shortage_at` IS NULL;
-- История до 20.09.2026: недостачи, записанные раньше, датируются днём
-- закрытия расчёта — так их и считал «Контроль». Идемпотентно: повтор
-- после обрыва ничего не меняет.
