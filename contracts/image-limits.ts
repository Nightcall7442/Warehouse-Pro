/* ═══════════════════════════════════════════════════════════════════════════
   Сколько знаков помещается в поле картинки.

   Логотип и значок вкладки хранятся строкой data:image/…;base64,… в столбцах
   типа TEXT — это 65 535 БАЙТ, а не «сколько понадобится». Строка длиннее не
   записывается: MySQL отклоняет запрос целиком.

   Числа стоят в contracts, потому что знать их обязаны обе стороны: клиент —
   чтобы сжать под них, сервер — чтобы отклонить словами. Пока предел знал
   только раздел «Брендинг», вкладка «Компания» жала логотип общей меркой (до
   семисот тысяч знаков) и клала его в такой же по устройству столбец.
   Сохранение падало, и вместе с логотипом не сохранялись ни название
   организации, ни адрес, ни банковские реквизиты; на экране при этом стояло
   безымянное «Внутренняя ошибка сервера».

   Здесь только числа и никакого DOM: файл читают и серверная проверка типов,
   и клиентская.
   ═══════════════════════════════════════════════════════════════════════════ */

/** Ёмкость столбца типа TEXT в MySQL, байт. */
export const TEXT_COLUMN_BYTES = 65_535;

export const LOGO_MAX_CHARS    = 60_000;
export const FAVICON_MAX_CHARS = 30_000;

/** Наибольшая сторона в точках — вторая половина того же ограничения. */
export const LOGO_MAX_DIMENSION    = 400;
export const FAVICON_MAX_DIMENSION = 64;
